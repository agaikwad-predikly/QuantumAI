import smtplibfrom email.mime.multipart import MIMEMultipartfrom email.mime.text import MIMETextfrom email.mime.base import MIMEBasefrom email import encodersimport osimport os.pathimport datetimedef mail_helper(to, cc, bcc, subject, body, attachments):	try:		fromaddr = "laxmikant.ainapure@predikly.com"		msg = MIMEMultipart() 		msg['From'] = fromaddr		msg['To'] = to		if cc is not None:			msg['CC'] = cc		if bcc is not None:			msg['BCC'] = bcc		msg['Subject'] = subject		body = body		msg.attach(MIMEText(body, 'html'))		 		if attachments is not None:			p = MIMEBase('application', 'octet-stream') 			p.set_payload((attachment).read())			encoders.encode_base64(p)			p.add_header('Content-Disposition', "attachment; filename= %s" % filename)			msg.attach(p)		s = smtplib.SMTP('smtp.gmail.com', 587)		s.starttls()		s.login(fromaddr, "laxmikantrordev@1493")		text = msg.as_string()			toaddrs = to.split(',') + cc.split(',') + bcc.split(',')		s.sendmail(fromaddr, toaddrs, text)		s.quit()		return True	except Exception as e:		return False"""filename = "ErrorLog"+datetime.date.today().date()+".log"		attachment = open(os.getcwd() + "\\" + filename, "rb")""""def send_eikon_failure_mail():	try:		return mail_helper('amit.gaikwad@predikly.com,laxmikant.ainapure@predikly.com',None,None,'Eikon proxy issue','<div style="margin: 0 auto; font-size:14px;">Their is an issue in ticker data pulling.</div><div style="margin: 0 auto; font-size:14px;"> Please restart the eikon proxy.</div>',None)	except Exception as e:		return Falsedef send_error_details_mail():	try:		return mail_helper('amit.gaikwad@predikly.com,laxmikant.ainapure@predikly.com',None,None,'Eikon proxy issue','<div style="margin: 0 auto : font-size: medium;">Please Find attached file for detail of errors..</div>',attachment)	except Exception as e:		return False